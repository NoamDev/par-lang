name: CI

on:
  push:
    branches: [ "test-ci" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: macos-15-intel
    steps:
      - uses: actions/checkout@v6
      - name: Run tests
        run: cd tests && cargo run test
